# DO NOT modify this file. Changes will be overwritten when there's a reset or update

# Name (case sensitive) of the profile that can be queried from a script
Name: "g++"

# (Optional) Name aliases (case sensitive) of the current profile
NameAliases: ["mingw"]

# The file extensions associated with the profile
FileExtensions: [.cpp, .cc, .cxx]

# The languages supported by the profile
Languages: ["c++"]

# (Optional) The commands to run in **shell** before calling the compiler/linker for each platform.
#            This is run inside the root build directory.
# Setup: 
#     DefaultPlatform: []

# (Optional) The commands to run in **shell** after calling the compiler/linker for each platform.
#            This is run inside the root build directory.
# Cleanup: 
#     DefaultPlatform: []

# The file properties for the object files for each platform. 
# See "./CommonFileTypes.yaml" for FilesTypes
# FilesTypes: ...

# (Optional) We can use the "Import" field to import other yaml files. We are importing "FilesTypes" here.
#            Import can either be a single path or a list of paths. 
#            All the fields in the imported yaml files will be merged together
Import: "./CommonFileTypes.yaml"

# Compiler settings, run once per input file
Compiler:
    # (Optional) The command to be prepend for each compile command in **shell** for each platform
    # PreRun: 
    #     DefaultPlatform: ""
    
    # Shell command to use for checking if the executable exists or not
    CheckExistence: 
        DefaultPlatform: "g++ -v"
    
    # Here are a list of substitution strings for RunParts, Setup and Cleanup. 
    # To escape '{' and '}' to avoid substitutioon, simply repeat the '{' or '}' character again.
    # So "${MyBashVariable}" will become "${{MyBashVariable}}"

    # Constants: --------------------------------------------------------------------------------------
    # {Stage.SharedLibraryFile.Prefix}
    # {Stage.SharedLinkFile.Prefix}
    # {Stage.StaticLinkFile.Prefix}
    # {Stage.ObjectLinkFile.Prefix}
    # {Stage.DebugSymbolFile.Prefix}
    # {Stage.SharedLibraryFile.Extension}
    # {Stage.SharedLinkFile.Extension}
    # {Stage.StaticLinkFile.Extension}
    # {Stage.ObjectLinkFile.Extension}
    # {Stage.DebugSymbolFile.Extension}
    # {/}:                                  Filesystem separator for the host platform
    
    
    # Stage Info: --------------------------------------------------------------------------------------
    # {Stage.Executable}:                   Compiler executable
    # {Stage.CompileFlags}:                 Compile flags from config and override
    
    
    # Input/Output Info: --------------------------------------------------------------------------------------
    # {Stage.InputFileName}:                Name of the current input source file (without directory path and extension)
    # {Stage.InputFileExtension}:           Extension of the current input source file
    # {Stage.InputFileDirectory}:           Directory of the current input source file
    # {Stage.InputFilePath}:                Full path to the current input source file
    # {Stage.OutputDirectory}:              Directory of all the output files
    
    
    # Iterable variables, must be inside "Repeats" run type: --------------------------------------------------------------------------------------
    # {Stage.DefineNameOnly}:               All the defines without a value specified (equivalent to #define X)
    # {Stage.DefineName}:                   Name of all the defines that has a value specified
    # {Stage.DefineValue}:                  Value of all the defines that has a value specified (use together with {Stage.DefineName})
    
    # {Stage.IncludeDirectoryPath}:         Path to all the include directories
    CompileTypes:
        Executable:
            DefaultPlatform:
                # Default flags to be substituted as {Stage.CompileFlags}
                Flags: "-std=c++17 -Wall -g"
                
                # The executable to be substituted as {Stage.Executable}
                Executable: "g++"
                
                # The components for the command to be run
                RunParts: &g++_CompileRunParts
                -   Type: Once
                    CommandPart: "{Stage.Executable} -c {Stage.CompileFlags}"
                -   Type: Repeats
                    CommandPart: " -D{Stage.DefineNameOnly}="
                -   Type: Repeats
                    CommandPart: " \"-D{Stage.DefineName}={Stage.DefineValue}\""
                -   Type: Repeats
                    CommandPart: " -I\"{Stage.IncludeDirectoryPath}\""
                -   Type: Once
                    CommandPart: " \"{Stage.InputFilePath}\" -o \"{Stage.OutputDirectory}{/}{Stage.ObjectLinkFile.Prefix}{Stage.InputFileName}{Stage.ObjectLinkFile.Extension}\""
                
                # What files to be expected as output for the command
                ExpectedOutputFiles: &g++_CompileExpectedOutputFiles
                -   "{Stage.OutputDirectory}{/}{Stage.ObjectLinkFile.Prefix}{Stage.InputFileName}{Stage.ObjectLinkFile.Extension}"
                
                # (Optional) The commands to run in **shell** BEFORE compiling
                #            This is run inside the .runcpp2 directory where the build happens.
                # Setup: []
                
                # (Optional) The commands to run in **shell** AFTER compiling
                #            This is run inside the .runcpp2 directory where the build happens.
                # Cleanup: []
        ExecutableShared:
            DefaultPlatform:
                Flags: "-std=c++17 -Wall -g -fpic"
                Executable: "g++"
                RunParts: *g++_CompileRunParts
                ExpectedOutputFiles: *g++_CompileExpectedOutputFiles
                # Setup: []
                # Cleanup: []
        Static:
            DefaultPlatform:
                Flags: "-std=c++17 -Wall -g"
                Executable: "g++"
                RunParts: *g++_CompileRunParts
                ExpectedOutputFiles: *g++_CompileExpectedOutputFiles
                # Setup: []
                # Cleanup: []
        Shared:
            DefaultPlatform:
                Flags: "-std=c++17 -Wall -g -fpic"
                Executable: "g++"
                RunParts: *g++_CompileRunParts
                ExpectedOutputFiles: *g++_CompileExpectedOutputFiles
                # Setup: []
                # Cleanup: []

# Linker settings, run once
Linker:
    CheckExistence:
        DefaultPlatform: "g++ -v"
    
    # Here are a list of substitution strings for RunParts, Setup and Cleanup
    
    # Constants: --------------------------------------------------------------------------------------
    # {Stage.SharedLibraryFile.Prefix}
    # {Stage.SharedLinkFile.Prefix}
    # {Stage.StaticLinkFile.Prefix}
    # {Stage.ObjectLinkFile.Prefix}
    # {Stage.DebugSymbolFile.Prefix}
    # {Stage.SharedLibraryFile.Extension}
    # {Stage.SharedLinkFile.Extension}
    # {Stage.StaticLinkFile.Extension}
    # {Stage.ObjectLinkFile.Extension}
    # {Stage.DebugSymbolFile.Extension}
    # {/}:                                  Filesystem separator for the host platform
    
    
    # Stage Info: --------------------------------------------------------------------------------------
    # {Stage.Executable}:                   Linker executable
    # {Stage.LinkFlags}:                    Link flags from config and override
    
    
    # Output Info: --------------------------------------------------------------------------------------
    # {Stage.OutputFileName}:               Name of all the output files (without directory path and extension)
    # {Stage.OutputDirectory}:              Directory of all the output files
    
    
    # Iterable variables, must be inside "Repeats" run type: --------------------------------------------------------------------------------------
    # {Stage.LinkFileName}:                 Name of the file to be linked, regardless of the build type
    # {Stage.LinkFileExtension}:            File Extension of the file to be linked, regardless of the build type
    # {Stage.LinkFileDirectory}:            Directory of the file to be linked, regardless of the build type
    # {Stage.LinkFilePath}:                 Full path to the file to be linked, regardless of the build type
    
    # {Stage.LinkObjectFileName}:           Name of the object file to be linked
    # {Stage.LinkObjectFileExtension}:      File Extension of the object file to be linked
    # {Stage.LinkObjectFileDirectory}:      Directory of the object file to be linked
    # {Stage.LinkObjectFilePath}:           Full path to the object file to be linked
    
    # {Stage.LinkSharedFileName}:           Name of the shared file to be linked
    # {Stage.LinkSharedFileExtension}:      File Extension of the shared file to be linked
    # {Stage.LinkSharedFileDirectory}:      Directory of the shared file to be linked
    # {Stage.LinkSharedFilePath}:           Full path to the shared file to be linked
    
    # {Stage.LinkStaticFileName}:           Name of the static file to be linked
    # {Stage.LinkStaticFileExtension}:      File Extension of the static file to be linked
    # {Stage.LinkStaticFileDirectory}:      Directory of the static file to be linked
    # {Stage.LinkStaticFilePath}:           Full path to the static file to be linked
    LinkTypes:
        Executable:
            Unix:
                Flags: "-Wl,-rpath,\\$ORIGIN"
                Executable: "g++"
                RunParts:
                -   Type: Once
                    CommandPart: "{Stage.Executable} {Stage.LinkFlags} -o \"{Stage.OutputDirectory}{/}{Stage.OutputFileName}\""
                -   Type: Repeats
                    CommandPart: " \"{Stage.LinkFilePath}\""
                ExpectedOutputFiles: ["{Stage.OutputDirectory}{/}{Stage.OutputFileName}"]
                # Setup: []
                # Cleanup: []
            Windows:
                Flags: "-Wl,-rpath,\\$ORIGIN"
                Executable: "g++"
                RunParts:
                -   Type: Once
                    CommandPart: "{Stage.Executable} {Stage.LinkFlags} -o \"{Stage.OutputDirectory}{/}{Stage.OutputFileName}.exe\""
                -   Type: Repeats
                    CommandPart: " \"{Stage.LinkFilePath}\""
                ExpectedOutputFiles: ["{Stage.OutputDirectory}{/}{Stage.OutputFileName}.exe"]
                # Setup: []
                # Cleanup: []
        ExecutableShared:
            DefaultPlatform:
                Flags: "-shared -Wl,-rpath,\\$ORIGIN"
                Executable: "g++"
                RunParts:
                -   Type: Once
                    CommandPart: "{Stage.Executable} {Stage.LinkFlags} -o \"{Stage.OutputDirectory}{/}{Stage.SharedLibraryFile.Prefix}{Stage.OutputFileName}{Stage.SharedLibraryFile.Extension}\""
                -   Type: Repeats
                    CommandPart: " \"{Stage.LinkFilePath}\""
                ExpectedOutputFiles: ["{Stage.OutputDirectory}{/}{Stage.SharedLibraryFile.Prefix}{Stage.OutputFileName}{Stage.SharedLibraryFile.Extension}"]
                # Setup: []
                # Cleanup: []
        Static:
            DefaultPlatform:
                Flags: ""
                Executable: "g++"
                RunParts:
                -   Type: Once
                    CommandPart: "{Stage.Executable} {Stage.LinkFlags} -o \"{Stage.OutputDirectory}{/}{Stage.StaticLinkFile.Prefix}{Stage.OutputFileName}{Stage.StaticLinkFile.Extension}\""
                -   Type: Repeats
                    CommandPart: " \"{Stage.LinkFilePath}\""
                ExpectedOutputFiles: ["{Stage.OutputDirectory}{/}{Stage.StaticLinkFile.Prefix}{Stage.OutputFileName}{Stage.StaticLinkFile.Extension}"]
                # Setup: []
                # Cleanup: []
        Shared:
            DefaultPlatform:
                Flags: "-shared -Wl,-rpath,\\$ORIGIN"
                Executable: "g++"
                RunParts:
                -   Type: Once
                    CommandPart: "{Stage.Executable} {Stage.LinkFlags} -o \"{Stage.OutputDirectory}{/}{Stage.SharedLibraryFile.Prefix}{Stage.OutputFileName}{Stage.SharedLibraryFile.Extension}\""
                -   Type: Repeats
                    CommandPart: " \"{Stage.LinkFilePath}\""
                ExpectedOutputFiles: ["{Stage.OutputDirectory}{/}{Stage.SharedLibraryFile.Prefix}{Stage.OutputFileName}{Stage.SharedLibraryFile.Extension}"]
                # Setup: []
                # Cleanup: []
