---
Dependencies:
  - Name: lib1
    LibraryType: static
    LibraryName: lib1
    Source:
        Type: git
        Value: "https://github.com/USER/REPO.git"
    Setup:
        Windows:
          - "mkdir build"
          - "cd build && cmake .."
          - "cd build && cmake --build . --config Release"
        Unix:
          - "mkdir build"
          - "cd build && cmake .."
          - "cd build && cmake --build . --config Release"